@model List<JMModels.Permission>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Index_Layout.cshtml";
}


<table id="Listp"></table>
<table id="List"></table>


@*!--自动DataGrid 从第一次加载与重置窗体大小时候发生的事件:分部视图-->*@
<script type="text/javascript">
    $(function () {
        $(window).resize(function () {
            $('#List').datagrid('resize', {
                width: $(window).width() - 10,
                height: $(window).height() - 35
            }).datagrid('resize', {
                width: $(window).width() - 10,
                height: $(window).height() - 35
            });
        });

    });
</script>

<script type="text/javascript">

    $(function () {
        $('#Listp').datagrid({
            url: '/SysRight/GetListP',
            width: $(window).width() - 10,
            height: $(window).height() - 35,
            methord: 'post',
            fitColumns: true,
            sortable: true,            
            sortName: 'KeyCode',
            sortOrder: 'desc',
            idField: 'KeyCode',
            pageSize: 15,
            pageList: [15, 20, 30, 40, 50],
            pagination: true,
            striped: true, //奇偶行是否区分
            singleSelect: true,//单选模式
            rownumbers: true,//行号
            columns: [[
                //{ field: 'Id', title: 'Id', width: 60 },
                //{ field: 'Nmae', title: 'Nmae', width: 60 },
                { field: 'KeyCode', title: 'KeyCode', width: 60 },
                { field: 'IsValid', title: 'IsValid', width: 100 }
            ]]
        });
    });
     

</script>


